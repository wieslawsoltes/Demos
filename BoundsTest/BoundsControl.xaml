<UserControl x:Class="BoundsTest.BoundsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600"
             FocusVisualStyle="{x:Null}">
    <Grid x:Name="layout" Background="WhiteSmoke" Width="600" Height="600" FocusVisualStyle="{x:Null}">
        <Canvas x:Name="drawingCanvas" Background="Transparent" FocusVisualStyle="{x:Null}"/>
        <Canvas x:Name="boundsCanvas" Background="{x:Null}" FocusVisualStyle="{x:Null}"/>
    </Grid>
</UserControl>
